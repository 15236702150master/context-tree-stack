# Listener (keep the sidecar on loopback when Nginx is the public entry point)
USAGE_SIDECAR_HOST=127.0.0.1
USAGE_SIDECAR_PORT=8098

# PostgreSQL connection used by the psql client
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432
DATABASE_USER=sub2api
DATABASE_DBNAME=sub2api
DATABASE_PASSWORD=
